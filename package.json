{
  "name": "nerdsync",
  "version": "0.17.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "node build.mjs",
    "check": "npm run test && npm run build",
    "deploy": "wrangler deploy --config ./wrangler.jsonc",
    "preview": "wrangler dev --config ./wrangler.jsonc",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "wrangler": "^4.125.0"
  }
}
